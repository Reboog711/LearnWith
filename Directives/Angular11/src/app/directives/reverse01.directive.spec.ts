import { Reverse01Directive } from './reverse01.directive';

describe('Reverse01Directive', () => {
  it('should create an instance', () => {
    const directive = new Reverse01Directive();
    expect(directive).toBeTruthy();
  });
});
