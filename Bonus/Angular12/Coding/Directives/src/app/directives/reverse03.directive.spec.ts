import { Reverse03Directive } from './reverse03.directive';

describe('Reverse03Directive', () => {
  it('should create an instance', () => {
    const directive = new Reverse03Directive();
    expect(directive).toBeTruthy();
  });
});
