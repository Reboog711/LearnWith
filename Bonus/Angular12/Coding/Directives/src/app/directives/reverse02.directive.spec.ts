import { Reverse02Directive } from './reverse02.directive';

describe('Reverse02Directive', () => {
  it('should create an instance', () => {
    const directive = new Reverse02Directive();
    expect(directive).toBeTruthy();
  });
});
